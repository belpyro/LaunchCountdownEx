namespace NASA_CountDown.StateMachine.Common
{
    public enum PluginEvents
    {
        OnInit,
        OnSettings,
        OnLauch,
        OnLaunched,
        OnSequence,
        OnFinish,
        OnDummy
    }
}